{
  "compilerOptions": {
    "baseUrl": ".",
    "composite": true, // Added
    "paths": {
      "@/*": ["./src/*"],
      "@common/*": ["../common/*"] // Recommended: so frontend can see common types too
    }
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}